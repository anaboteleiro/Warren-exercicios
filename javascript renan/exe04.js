/**
 * Crie um script usando estrutura condicional (IF...ELSE) que deve verificar:
 * - se o valor da variável {numero} está entre o valor da variável {inicio} e o valor da variavel {final}
 * - caso esteja dentro do intervalo (maior ou igual a inicio E menor ou igual a final), exiba uma mensagem
 *    que o número está dentro deste intervalo;
 * - caso este número esteja fora do intervalo, exiba uma mensagem que o número está fora do intervalo;
 */

 let numero = 10;
 let inicio;
 let final;
 
 // escreva seu código aqui
 if (numero < 0) {
    console.log("inicio");
} else {
    console.log("final");
}