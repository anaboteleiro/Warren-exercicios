let primeiroNumero = 10;
let segundoNumero = 20;
let operacao = "somar";

if (operacao == "somar") {
  const calculo = primeiroNumero + segundoNumero;
  console.log(`Soma de ${primeiroNumero} e ${segundoNumero} é ${calculo}`);
}